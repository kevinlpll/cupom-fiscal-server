const { AUTH_ALGORITHM, AUTH_PRIVATE_KEY, NAME, PORT, IP } = require('./_variables')

const globals = {
  AUTH_ALGORITHM, 
  AUTH_PRIVATE_KEY,
  NAME,
  PORT,
  IP,
   _genders: ['female', 'male', 'other']
}

export default globals 